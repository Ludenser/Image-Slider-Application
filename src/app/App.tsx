import ImageSlider from '../components/ImageSlider/ImageSlider';

export const App = () => {
    return (
        <div className="App">
            <ImageSlider previewCount={7} />
        </div>
    );
};
